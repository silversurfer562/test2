# Plan: Temporal staleness

**Spec**: [docs/specs/temporal-staleness/](../../docs/specs/temporal-staleness/)
**Created**: 2026-09-11

---

## Summary

attune can prove a memory survives process death. It cannot prove a memory that went out of date loses the slot it used to win. `scripts/memory_recall_eval.py` has `--phase all|persistence|capture|evaluate`; `run_persistence_benchmark()` forks a capture subprocess and then an evaluate subprocess against one `tempfile.mkdtemp()` root and reads results back through a file. That is durability testing and it is real. It measures nothing about time. The scored metrics in `evaluate()` are `hit_at_1`, `hit_at_3`, and two lists of top-1 scores — every one of them asks "could the right thing be found", none asks "what was sitting above it".

The claim with no test is the two-layer memory protocol ratified 2026-07-02 in `attune-ai-dev/discipline/COLLABORATION_DISCIPLINE.md` §5 (lines ~684-710): stale operational memory is worse than none and its regime is machine verification at load, every time; stale durable memory fails softly and its regime is human review over time. The durable half is shipped — `scripts/review_curated_memory.py` runs the keep/wrong/sharper verdict loop against `src/attune/memory/verdict_log.py`, and `scripts/audit_curated_memory.py` sweeps advisory and exits 0 on purpose. The operational half has no machine verification at load anywhere, and no measurement that would notice its absence.

Three things reading the code changed about the brief, and the plan is built on the code. First, the staleness subsystem already exists: `src/attune/memory/curated_audit.py` is 815 lines with `resolve_age_basis()` (line 361), `unverified_age_days(mem, today=...)` (line 401), `epistemic_tier()` (line 506) against `TIER_SETTLED_MAX = 10.0` / `TIER_CHECK_MAX = 45.0`, and `PersonalMemory.query()` already calls `_annotate_staleness()` on every hit. This plan reuses that model and does not build a second one; the brief's citation of `patterns/confidence.py::get_stale_patterns(days=90)` is wrong twice over — the real symbol is `src/attune/patterns/confidence.py::PatternConfidenceTracker.get_stale_patterns` (a bound method at line 261, `days: int = 90`), the root-level `patterns/` directory has no `confidence.py` at all, and pattern usage recency is unrelated to memory staleness either way. Second, `PersonalMemory.capture()` writes through `atomic_write_text`, which does `tmp.replace(path)` — re-capturing the same topic and kind leaves exactly one file, so "fact stated session 1, changed session 3" as the brief describes it has nothing left to lose the slot to. SC1 and SC3 fixtures must use distinct topic slugs or the scenarios measure nothing. Third, the memory path is not an undefended door: `src/attune/memory/provenance.py` wraps every recalled hit in an untrusted-evidence envelope and `mcp/memory_handlers.py` strips prose so only the envelope reaches the model. The genuine defect next door is that `_stamp_provenance()` builds `context_block` from `str(hit.get("excerpt") or hit.get("summary") or "")` (personal.py:342), neither of which carries the age `_annotate_staleness()` computed one line earlier — and `cli_commands/memory_commands.py::cmd_memory_recall` prints `hit["summary"]` and `hit["excerpt"]` raw, with neither envelope nor age.

Shipping this changes one product claim from asserted to measured: that a recalled memory arrives with its age attached, and that ageing a memory changes how it is labelled without changing where it ranks. The gate is deliberately NOT "did the current fact win slot 1" — `attune-rag/src/attune_rag/retrieval.py::KeywordRetriever.retrieve` scores on token overlap times `CATEGORY_WEIGHTS` with no mtime, recency or verified term anywhere, and `PersonalMemory.query()` sorts on that score plus a +0.001 project tiebreak, so a hand-authored winner case is a function of the fixture author's vocabulary and can go green while the claim stays false. The gated case is instead a DIFFERENTIAL over age with wording held constant: capture the competing pair, query, record slot 1 and the annotations, then `os.utime` only the superseded file and re-query. Slot-1 identity must be UNCHANGED (that pins the ruled label-never-reorder behaviour of `docs/specs/memory-status-integrity` D1, and goes red the day anyone sorts by age), and the annotation and tier on the aged hit must have CHANGED as expected. Alongside it, the envelope-coverage metric — every returned hit carries a non-empty status string inside `provenance.context_block` — is gated by equality, because the age path fails silently today (`_annotate_staleness` swallows `(KeyError, OSError, ValueError)` and `_resolve_hit_path` returns `None` and continues). Slot-1 occupancy for the hand-authored winner cases, and hit@k over the temporal set, are reported as DIAGNOSTICS and are explicitly not pass conditions.

---

## Tasks

<task id="T1" name="Spec home, ruled semantics, pre-committed matrix">
  <objective>
    Create the spec home and rule the semantic questions before any code exists, so none of them get settled by accident during implementation. Run `python scripts/collaboration_preflight.py` first — the AGENTS.md session protocol requires it before non-trivial work. Decide and record: (a) the work lives in a new docs/specs/temporal-staleness/ rather than extending memory-recall-eval, because .github/workflows/spec-status-reminder.yml keys off the directory name (SPEC_REF = re.compile of the pattern for an optional docs/ prefix then specs/ then a lowercase-hyphen slug, at line 58) and posts ONE reminder comment per merged PR that references an in-flight spec — a merged-PR reminder, not a gate; the changelog gate is orthogonal and has no spec-directory logic at all, so it does not bear on this choice; (b) SC1 and SC3 scenarios use DISTINCT topic slugs, because capture() replaces root/TOPIC/KIND.md destructively and a same-slug re-capture leaves no competitor; (c) simulated sessions are aged with os.utime, because _build_skeleton() writes no frontmatter, load_memory() therefore returns verified=None, and resolve_age_basis() falls to the "mtime" basis at DEFAULT_VOLATILITY 0.75; (d) the gated metric is the AGE DIFFERENTIAL (slot-1 identity stable across an os.utime flip, annotation changed) plus envelope age-coverage by equality, while hand-authored slot-1 occupancy and hit@k over the temporal set are stated non-metrics reported as diagnostics. OQ1 rules the scenario slug shape: lowercase-hyphen only, matching the shape spec-status-reminder.yml's SPEC_REF accepts, prefixed sc1- / sc2- / sc3- / sc4- so scenario-class ids can never collide with the task ids T1..T8 that D1's must_pass_cases and the dep graph key on, and never equal to a key of attune_rag.retrieval.KeywordRetriever.CATEGORY_WEIGHTS (concepts 1.5, quickstarts 1.5, tasks 1.2, references 1.0, comparisons 1.0, tips 0.9 and the rest), because DirectoryCorpus infers category from the first path segment and a colliding slug silently multiplies the very margin the gate scores. OQ4 rules the gate's landing posture and whether the temporal round-trip joins scripts/release_recall_gate.py. Write D1 as a PRE-COMMITTED matrix naming the specific must-pass cases by sc-prefixed id, never a percentage.
  </objective>
  <files-to-create>
    <file path="docs/specs/temporal-staleness/requirements.md">Opens with an in-vocabulary status line, verbatim shape `**Status:** draft (2026-09-11) — pre-implementation` (see the validation checks: the leading token must be in attune.ops.spec_lifecycle.STATUS_VOCABULARY). Then Problem, Goal, Approach, Requirements as "- **R1 - Title.**" bullets, Non-goals, Done when. R1 isolation (explicit tmp roots only, and no scenario slug reaching any path under the developer's ATTUNE_HOME), R2 realistic scenario content, R3 hand-authored ground truth not derived from the system under test, R4 the age differential plus envelope age-coverage as the gated metrics, R5 one clean report, R6 no second age model.</file>
    <file path="docs/specs/temporal-staleness/design.md">Opens with the same in-vocabulary `**Status:** draft (2026-09-11) — ...` line. Architecture as an ASCII text block showing the temporal phase orchestrating two leaf subprocesses. OQ1 scenario slug shape (lowercase-hyphen, sc-prefixed, disjoint from CATEGORY_WEIGHTS, with the category-multiplier reason recorded as the justification), OQ2 session forgery mechanism, OQ3 conflict presentation, OQ4 gate shape and landing posture, each with lettered options and a RECOMMENDED marker. Mirrors docs/specs/memory-recall-eval/design.md.</file>
    <file path="docs/specs/temporal-staleness/decisions.md">Opens with the same in-vocabulary `**Status:** draft (2026-09-11) — ...` line. D1 headed "D1 - Temporal go/no-go matrix (PRE-COMMITTED 2026-09-11)" with a Result/Decision table naming every must-pass case id before any case runs, in the sc- prefix ruled by OQ1: the sc1-changed and sc3-contradiction age-differential cases, the sc4-injected high-signal case, the sc4-injected curated-imperative by-design non-flag, the sc4-injected past-the-excerpt-window non-flag, and the envelope-age-coverage case (every returned hit carries a non-empty status string inside provenance.context_block). Carries the ams-int8-quantization clause "Numbers revisitable only with stated reason before the run, never after", and — instead of the forgeable "the commit timestamp is the arbiter" formula — the CONTENT binding: "The arbiter is this file's blob sha, recorded in thresholds.json; raising a threshold or dropping a case requires editing D1 and shows up in the diff." Committer dates are rewritten by every rebase and amend and settable via GIT_COMMITTER_DATE, so they arbitrate nothing.</file>
  </files-to-create>
  <files-to-modify>
    <file path="docs/specs/memory-recall-eval/design.md">Add one line to the Architecture block recording that scripts/memory_recall_eval.py, not the spec-named scripts/eval_personal_memory_recall.py, carries the multi-process phase architecture and is therefore where the temporal phase lands. Reconciles the two harnesses in writing instead of leaving the split undocumented.</file>
  </files-to-modify>
  <validation>
    <check>ls docs/specs/temporal-staleness/ lists requirements.md, design.md and decisions.md. All three are canonical phase files under src/attune/ops/specs_data.py::_PHASE_FILES, so all three are swept: each must open with a `**Status:**` line whose LEADING TOKEN is in attune.ops.spec_lifecycle.STATUS_VOCABULARY (the complete vocabulary is complete, completed, done, shipped, superseded, approved, active, living, draft, parked, paused). Write `**Status:** draft (2026-09-11) — pre-implementation`. A token like ratified, proposed or "in progress" turns the suite red on the first commit of the workstream; `parked` additionally requires a literal `Resume-Trigger:` clause in the same file (RESUME_TRIGGER_CLAUSE in src/attune/gates/lifecycle/status_line.py).</check>
    <check>python -m pytest tests/unit/gates/test_status_line_gate.py -q passes with the new directory present. test_corpus_sweep_every_spec_dir_is_legible parametrizes over every directory under docs/specs/ carrying a phase file and asserts status_line_gate(spec_dir).state == "PASS"; src/attune/gates/lifecycle/status_line.py is the gate it calls. RED if any of the three files carries an out-of-vocabulary token, GREEN when all three are in vocabulary.</check>
    <check>grep -nE "[0-9]+%|rate" docs/specs/temporal-staleness/decisions.md shows no pass condition expressed as a percentage; the D1 table rows name sc-prefixed case ids.</check>
    <check>grep -nE "\bT[1-8]\b" docs/specs/temporal-staleness/decisions.md shows T1..T8 used only as task references, never as a scenario-class id — every scenario id in the D1 matrix carries the sc prefix.</check>
    <check>grep -n "hit@" docs/specs/temporal-staleness/requirements.md shows hit@k appearing only under Non-goals.</check>
    <check>git hash-object docs/specs/temporal-staleness/decisions.md prints the blob sha that T6 records in thresholds.json as d1_blob_sha256; capture it in the commit message so the binding is reconstructible.</check>
  </validation>
  <risks>
    <risk severity="medium">Writing D1 as a rate over four scenario classes repeats the error the 2026-08-28 memory-recall-eval entry names explicitly ("2 of 6 must NOT be read as a 33% false-positive rate ... the Wilson interval is roughly 4%-78%"). The matrix must be a named per-case table.</risk>
    <risk severity="medium">Choosing a new spec directory splits memory-eval history across two homes. Mitigated by the cross-reference line added to memory-recall-eval/design.md.</risk>
    <risk severity="low">A scenario slug that collides with a CATEGORY_WEIGHTS key moves the measured margin silently. OQ1 rules the disjointness and T2 makes check_temporal_fixture() enforce it, so the rule cannot decay into a comment.</risk>
  </risks>
</task>

<task id="T2" name="Temporal fixture with age differentials, expected winners and ages">
  <objective>
    Author TEMPORAL_SCENARIOS as a hand-written data table beside the existing CORPUS and QUERIES in scripts/memory_recall_eval.py. Each scenario carries a list of captures (topic, kind, content, session_index), a per-capture age in days used to drive os.utime, the query text, the topic that is expected to occupy slot 1, and the topic(s) expected not to. Slugs are lowercase-hyphen and sc-prefixed per T1's OQ1. Four classes. sc1-changed: a fact under one topic at session 1, a superseding fact under a DIFFERENT topic at session 3, queried at session 5. sc2-expired: a deadline or status whose date has passed. sc3-contradiction: two topics with incompatible claims and deliberately matched keyword overlap, so the scores tie and the tiebreak is what is being measured. sc4-injected: three sub-cases keyed to the 200-character excerpt window (attune_rag/provenance.py:193, excerpt_chars default 200, never overridden by pipeline.py) minus the roughly 20 characters _build_skeleton prepends as "# TOPIC" and "## Decision" — one high-signal payload (role-delimiter or tool-invocation shaped) placed at a RECORDED character offset INSIDE the window, which MUST carry an instruction flag on every untrusted tier; one bare curated imperative inside the window which MUST NOT flag, because _DIRECTIVE_PATTERNS apply only to UNTRUSTED_TIERS and _stamp_provenance stamps tier="curated"; and one high-signal payload placed at a recorded offset PAST the window, whose expected outcome is "not flagged — outside the excerpt window", so the limit is measured rather than accidentally avoided. Additionally, sc1-changed and sc3-contradiction each carry a DIFFERENTIAL pairing: the same two captures with wording held constant, an aged variant produced by os.utime on the superseded file ONLY, and the expectation that slot-1 identity is unchanged between the two queries while the aged hit's annotation and tier change. That differential is the gated case; the hand-authored winner field is a diagnostic. Add check_temporal_fixture() which runs before any scoring and exits nonzero on drift.
  </objective>
  <files-to-modify>
    <file path="scripts/memory_recall_eval.py">Add a TemporalCapture and TemporalScenario dataclass pair and the TEMPORAL_SCENARIOS table after QUERIES. Add check_temporal_fixture() which verifies: every expected-winner and must-not-win topic appears in the scenario's own capture list; every topic slug is unique within a scenario; every slug matches the lowercase-hyphen sc-prefixed shape ruled in OQ1; no slug is a key of attune_rag.retrieval.KeywordRetriever.CATEGORY_WEIGHTS; every sc4 payload offset is recorded and its inside-window / past-window classification agrees with the 200-character excerpt limit; and — before any capture runs — every scenario's content passes a dry secret scan through attune.memory.short_term.security.DataSanitizer (or a dry attune.memory.personal._secret_gate call in a try block), exiting nonzero and naming the offending scenario id. Returns a nonzero exit before any number is printed, mirroring scripts/phase0/lessons_rag_benchmark.py. This file has NO licence header by design — match the file, do not add one.</file>
  </files-to-modify>
  <validation>
    <check>python -c "import importlib.util,sys;s=importlib.util.spec_from_file_location('m','scripts/memory_recall_eval.py');m=importlib.util.module_from_spec(s);sys.modules['m']=m;s.loader.exec_module(m);print(len(m.TEMPORAL_SCENARIOS))" prints a count of at least 5, and the four class prefixes sc1- / sc2- / sc3- / sc4- each appear at least once in the scenario ids. RED before this change (AttributeError), GREEN after.</check>
    <check>python -c "... exec_module as above ...; import sys; sys.exit(m.check_temporal_fixture())" exits 0 against the real table. The failure modes (expected_winner absent from the capture list, duplicate (topic, kind), competitors sharing a slug, a slug colliding with CATEGORY_WEIGHTS, a secret-shaped payload) are pinned as unit tests over synthetic scenario objects in T7 — the real table is never hand-edited to demonstrate a failure.</check>
    <check>python -c "... exec_module as above ...; print([c.offset for s in m.TEMPORAL_SCENARIOS if s.id.startswith('sc4-') for c in s.captures])" prints a recorded character offset for every sc4 payload, and at least one is past 200.</check>
  </validation>
  <risks>
    <risk severity="medium">Hand-matching keyword overlap for sc3-contradiction so the scores actually tie is fiddly against an unbounded overlap count. If they do not tie, the scenario measures ranking, not tiebreaking; record the observed margin rather than tuning the fixture until it produces the wanted answer. The gated half of sc3 is the age differential, which holds wording constant and is immune to this.</risk>
    <risk severity="medium">A secret-shaped payload does not get quietly redacted. src/attune/memory/personal.py::_secret_gate RAISES on a detected critical/high secret — "capture is a deliberate action, so a detected secret RAISES: the caller must know their content was refused" — and fails closed with an error if DataSanitizer cannot be imported; only PII is redacted in place. capture() calls it unconditionally before the write. An sc4 payload carrying high-entropy tokens therefore aborts the whole fixture write mid-loop with a traceback. The pre-scoring dry scan in check_temporal_fixture() turns that into a named fixture error with the offending scenario id.</risk>
  </risks>
  <dependencies>
    <dep>T1</dep>
  </dependencies>
</task>

<task id="T3" name="Carry age into the model-facing envelope">
  <objective>
    Fix the concrete defect that makes conflict presentation undecidable today. In PersonalMemory.query(), _annotate_staleness() stamps unverified_days, staleness and status onto each hit; _stamp_provenance() then runs and builds context_block from `str(hit.get("excerpt") or hit.get("summary") or "")` (personal.py:342) — and query() sets both keys to the same value ("summary": hit.excerpt or "", "excerpt": hit.excerpt or "", personal.py ~296-297), so both are attune_rag's excerpt and neither carries the age. wrap_recalled() has no parameter for age. The MCP handler returns that context_block as the model-facing "context" and strips summary and excerpt from the structured results, so the model receives prose with no age in one field and age with no prose in another. Add an optional status/age argument to wrap_recalled(), thread it through provenance_fields() and _stamp_provenance(), and implement the ruled presentation: both conflicting memories reach the model, each labelled with its own age and epistemic tier, in the order query() already produced. Do not drop or reorder by age — docs/specs/memory-status-integrity/decisions.md:8 D1 forecloses that. src/ enforces ruff B904: any re-raise added here must use `raise ... from e`. Secondary: bring cmd_memory_recall's human path into line, or record in decisions.md why the CLI stays raw.
  </objective>
  <files-to-modify>
    <file path="src/attune/memory/provenance.py">wrap_recalled() gains a keyword-only status argument (the format_status_annotation string, or None). When present it renders inside the recalled_memory envelope as an explicit age line. provenance_fields() gains the same pass-through so context_block carries it. Docstrings cite docs/specs/temporal-staleness R4.</file>
    <file path="src/attune/memory/personal.py">_stamp_provenance() reads hit["status"] and hit["unverified_days"] (already stamped by _annotate_staleness, which runs first) and passes them into provenance_fields(). Keep the narrow (KeyError, TypeError, ValueError) handler and the label-never-reorder posture. B904 applies to any new raise site.</file>
    <file path="src/attune/cli_commands/memory_commands.py">cmd_memory_recall's human path prints hit["summary"] and hit["excerpt"] raw. Print the staleness/status label alongside them so the human surface and the MCP surface agree about what a recalled memory looks like.</file>
    <file path="tests/unit/memory/test_provenance.py">Add cases for the status argument on wrap_recalled — presence, rendering, and the None default. Keep the module's stated posture: assert framing and labelling, never that a payload is neutralised. This entry owns the status-argument cases; T7 adds only the curated-tier by-design non-flag case to this file.</file>
    <file path="tests/unit/memory/test_personal_memory.py">Pin that a hit's provenance context_block contains the same status string that _annotate_staleness stamped on the hit, and that "status" is PRESENT and non-empty on every hit returned from a tmp-root query — not merely consistent-if-present.</file>
  </files-to-modify>
  <validation>
    <check>python -c "import inspect,sys;sys.path.insert(0,'src');from attune.memory.provenance import wrap_recalled;assert 'status' in inspect.signature(wrap_recalled).parameters" exits 0. RED before this change, GREEN after — but this alone is satisfied by an unused parameter, so it is a signature pin only and the next check is the real one.</check>
    <check>Capture one memory into a tmp root, query it, and assert all three: "status" in hit; hit["status"] is a non-empty string; hit["status"] appears verbatim in hit["provenance"]["context_block"]. Presence is required because _annotate_staleness swallows (KeyError, OSError, ValueError) and _resolve_hit_path returns None and continues, so a check phrased as `hit.get("status","") in ctx` is vacuously true when the age path silently produced nothing. RED before this change, GREEN after.</check>
    <check>Call _handle_personal_memory_recall against the same tmp root and assert the returned dict's "context" value contains that same non-empty status string. render_recall_for_context is what actually reaches the model; the structured results have summary and excerpt stripped, so this is the only surface where the model can see the age.</check>
    <check>Capture two memories into a tmp root, run `attune memory recall` against that root with ANTHROPIC_API_KEY="" and an isolated ATTUNE_HOME, and assert both that a staleness label appears and that the number of printed result lines is greater than zero. Against the developer's real home an empty or non-matching corpus prints "No results found." and returns 0, which satisfies a label assertion vacuously.</check>
    <check>python -m pytest tests/unit/memory/test_provenance.py tests/unit/memory/test_personal_memory.py -q passes.</check>
  </validation>
  <risks>
    <risk severity="high">render_recall_for_context() is documented in-code as "the R1 boundary - the CONSUMER CONTRACT" and its live consumer is plugin/hooks/session_recall.py::_format. Changing the envelope text changes what every session injects. A human should read the rendered before/after side by side before this merges.</risk>
    <risk severity="medium">Adding an age line lengthens every envelope and consumes context on every recall. Measure the added characters per hit and state the number in decisions.md rather than asserting it is small.</risk>
    <risk severity="medium">The status label is computed from file mtime for captured personal memories (no frontmatter, so resolve_age_basis returns the "mtime" basis). The envelope must not imply the claim was verified on that date. Render the basis, not just the date.</risk>
  </risks>
  <dependencies>
    <dep>T1</dep>
  </dependencies>
</task>

<task id="T4" name="--phase temporal on the existing multi-process architecture">
  <objective>
    Add "temporal" to the --phase choices in scripts/memory_recall_eval.py by mirroring run_persistence_benchmark(), not by writing a second harness. A tmp root under tempfile.mkdtemp(), a capture-side subprocess that writes the scenario captures and applies os.utime per each capture's session age, then a separate evaluate-side subprocess starting from a brand-new PersonalMemory against the same on-disk root, with results handed back through --json-out to a FILE — never stdout, because attune_rag's structlog PrintLogger writes there and corrupts inline JSON, as the existing code documents at run_persistence_benchmark. Reuse --root. Name the leaf phases capture-temporal and evaluate-temporal rather than overloading capture/evaluate with a --scenario-set flag, so the existing corpus phases keep their exact behaviour and the "requires --root" guard at line 392 is GENUINELY extended from ("capture", "evaluate") to include the two new names. Determinism of the on-disk corpus is part of this task: capture_temporal() must point ATTUNE_AUTHOR_POLISH_CACHE at a directory inside the run's tmp root and set ATTUNE_AUTHOR_STRICT_POLISH=0, because polish_template() consults ~/.attune/polish_cache BEFORE any LLM call and returns the bare skeleton when the cache misses and strict is off — a warm developer cache yields polished docs and a cold CI runner yields skeletons, the exact mechanism docs/specs/memory-recall-eval/decisions.md located on 2026-08-28 ("run 1's corpus was UNPOLISHED ... keyword retrieval is measurably less precise on the richer text"), which would give different text, different scores and different slot-1 winners from the same table. Isolation of the developer's home is also part of it: point ATTUNE_HOME at the tmp root too, because PersonalMemory.query() calls log_curated_recall(..., surface="personal_query") on every query and serve_telemetry writes to $ATTUNE_HOME/telemetry/memory_events.jsonl, which feeds the review-ranking frequency term. Fix the module docstring's "~/.attune/personal_memory" to the real default "~/.attune/memory" (src/attune/memory/personal.py line 32) in the same commit. Extend print_report() with a temporal block.
  </objective>
  <files-to-modify>
    <file path="scripts/memory_recall_eval.py">Add capture_temporal(global_root), evaluate_temporal(global_root) and run_temporal_benchmark(); add "temporal", "capture-temporal" and "evaluate-temporal" to the --phase choices; extend main()'s dispatch and extend the guard at line 392 from `if args.phase in ("capture", "evaluate") and args.root is None` to include the two temporal leaf phases; extend print_report(). main()'s temporal branch writes json.dumps(results) to args.json_out when given and only prints the report otherwise — the same shape as the evaluate branch at lines 399-403, which is today the ONLY branch that honours --json-out (the persistence and all branches ignore args.json_out entirely). Without this the ORCHESTRATOR cannot emit the dump that T6's checker and T8's CI job consume. The write errors loudly on an unwritable path rather than falling through to a printed report. Stamp generated_at (UTC ISO-8601) and the current git HEAD sha into the dump, plus a polish_state field recording the pinned cache directory and strict setting, so the checker can refuse a stale or foreign dump. evaluate_temporal() enforces an instrument-health precondition before any scoring: every scenario must return at least one hit and at least as many hits as it has competing captures, surfaced as instrument_ok in the dump — `_load_rag()` returning None makes query() log a warning and return [] for every scenario, and zero hits everywhere is an instrument failure that must not reach the checker looking like an ordinary regression. This file has NO licence header by design — match the file, do not add one.</file>
  </files-to-modify>
  <validation>
    <check>ANTHROPIC_API_KEY="" python scripts/memory_recall_eval.py --phase temporal --json-out /tmp/temporal_dump.json writes the file, and the dump's instrument_ok is true, hits_total is greater than zero, generated_at and git_head are present, and every scenario id in TEMPORAL_SCENARIOS appears in the per-scenario records. RED before this change (argparse rejects the choice and exits 2), GREEN after. Exit code 0 alone is NOT the check — a run where every query returns [] also exits 0.</check>
    <check>python scripts/memory_recall_eval.py --phase capture-temporal exits nonzero with "requires --root", and so does --phase evaluate-temporal. RED before this change (argparse rejects the unknown choice with a different message and exit 2), GREEN after, because line 392's tuple genuinely gains the two names.</check>
    <check>grep -n "personal_memory" scripts/memory_recall_eval.py returns nothing. RED before (line 20), GREEN after.</check>
    <check>ANTHROPIC_API_KEY="" python scripts/memory_recall_eval.py --phase all still prints the same hit@1 and hit@3 counts as before the change — the existing phases are untouched.</check>
    <check>After a full temporal run, no scenario topic slug appears anywhere under the developer's ATTUNE_HOME — not in memory/, not in polish_cache/, not in telemetry/memory_events.jsonl — nor under the repo's .attune/memory: a recursive, case-insensitive grep for each scenario slug (grep -rci) over both trees matches nothing. Case-insensitive on purpose: with a case mismatch between the fixture slug and the check pattern, a count of 0 means the pattern missed, not that nothing leaked.</check>
    <check>grep -n "ATTUNE_HOME\|ATTUNE_AUTHOR_POLISH_CACHE\|ATTUNE_AUTHOR_STRICT_POLISH" scripts/memory_recall_eval.py shows all three pinned inside capture_temporal and evaluate_temporal, and the dump's polish_state records what they were set to.</check>
  </validation>
  <risks>
    <risk severity="medium">os.utime on the captured file sets mtime, but PersonalMemory may re-derive summaries or touch the file on a later write. Apply the utime after every capture in the scenario completes, and assert the aged mtime survives into the evaluate process.</risk>
    <risk severity="medium">At DEFAULT_VOLATILITY 0.75 every captured personal memory crosses check-before-acting at roughly 14 days and suspect at roughly 60 days on file age alone. Session ages must be chosen deliberately against those constants, and the report must not present a tier label as evidence the fact changed.</risk>
    <risk severity="low">scripts/ is in pyproject.toml's ruff exclude list, so nothing lints this file. Match the surrounding style by hand.</risk>
  </risks>
  <dependencies>
    <dep>T2</dep>
    <dep>T3</dep>
  </dependencies>
</task>

<task id="T5" name="Age-differential scoring, envelope coverage, and the recall-is-not-staleness guard">
  <objective>
    Implement the metrics that answer the actual question. GATED, per differential case: query the competing pair, record slot 1 and both annotations, os.utime ONLY the superseded file, re-query, and assert that slot-1 identity is UNCHANGED (age_differential_slot1_stable) while the aged hit's status annotation and epistemic tier CHANGED as expected (age_differential_annotation_changed). That varies time with wording held constant, is red before T3 and green after, and goes red the day anyone makes ranking time-aware — which memory-status-integrity D1 forbids. GATED, per run: hits_with_age_in_envelope must equal hits_total, where a hit counts only if its provenance context_block contains a non-empty status string; the age path fails silently today, so a run in which the age reached zero hits must not be indistinguishable from a healthy one. DIAGNOSTIC, not pass conditions: the hand-authored slot1_current_fact_wins count (ranking is time-blind — score is token overlap times CATEGORY_WEIGHTS plus a +0.001 project tiebreak, so a hand-authored winner measures the fixture author's vocabulary), the per-case margins, and hit@1/hit@3 over the temporal set, each printed under a header carrying the one-line reason. For any tie-break judgement use a within-run relative criterion in the shape of the D3 rule already ruled in docs/specs/memory-recall-eval — no absolute cutoff, because the script's own comment establishes that score is an unbounded raw keyword-overlap count with no universal threshold. Make the report structurally incapable of the trap: no pass boolean may read any hit@k value, and a run with perfect hit@3 and a broken age envelope must come out FAILED.
  </objective>
  <files-to-modify>
    <file path="scripts/memory_recall_eval.py">evaluate_temporal() returns per-scenario records with slot1_topic_before, slot1_topic_after, expected_winner, margin, both annotations before and after the utime flip, hits_total, hits_with_age_in_envelope, and a passed boolean derived ONLY from the differential and envelope-coverage conditions. The returned dict carries age_differential_slot1_stable, age_differential_annotation_changed, hits_total, hits_with_age_in_envelope, must_pass_cases, instrument_ok, generated_at, git_head, polish_state, and a separate diagnostic block holding slot1_current_fact_wins, the margins and hit@1/hit@3. print_report()'s temporal block prints the diagnostic under its own header carrying the non-pass-condition sentence. This file has NO licence header by design — match the file, do not add one.</file>
  </files-to-modify>
  <validation>
    <check>Construct a synthetic results dict where every scenario has the current fact in the top 3 and slot-1 identity stable but hits_with_age_in_envelope less than hits_total, pass it to the report path, and the overall verdict is FAILED. RED before this change (no such code path), GREEN after — pinned as a test in T7.</check>
    <check>Construct a second synthetic results dict where slot-1 identity CHANGED across the utime flip; the verdict is FAILED even though every current fact is present at k=3. Pinned as a test in T7.</check>
    <check>The JSON dump from --phase temporal --json-out contains age_differential_slot1_stable, age_differential_annotation_changed, hits_total, hits_with_age_in_envelope, must_pass_cases, instrument_ok and a diagnostic key, and the diagnostic key holds slot1_current_fact_wins and the hit@k numbers.</check>
    <check>grep -n "DIAGNOSTIC" scripts/memory_recall_eval.py shows the label on both the hit@k and the slot1_current_fact_wins output paths.</check>
    <check>No scenario's pass boolean is computed from any hit@k value or from slot1_current_fact_wins: grep the pass computation and confirm it reads only the differential comparison and the envelope-coverage count.</check>
  </validation>
  <risks>
    <risk severity="medium">Scoring on the status annotation alone measures when the file was last written, not whether the fact changed. The differential controls for that by holding wording constant and varying only mtime; annotations are compared before and after, never read as evidence a fact changed.</risk>
    <risk severity="medium">If the two competing topics tie exactly, slot 1 is decided by the sort's stability and the +0.001 project tiebreak. Report exact ties as a distinct outcome rather than folding them into pass or fail; the differential still holds, because a stable sort returns the same order for identical scores across both queries.</risk>
  </risks>
  <dependencies>
    <dep>T4</dep>
  </dependencies>
</task>

<task id="T6" name="Port the threshold gate and lock the temporal baseline">
  <objective>
    attune-ai has no thresholds file and no check_thresholds equivalent — a search for threshold data files across attune-ai, attune-forms and attune-verify returns none. precision_at_1 gates from attune-rag/docs/specs/release-quality-baseline/thresholds.json via attune-rag/scripts/check_thresholds.py at attune-rag/.github/workflows/benchmark.yml line 158, with attune-rag/tests/unit/test_check_thresholds.py as the test template to port alongside it. Port the same MECHANISM with a deliberately DIFFERENT metric block — not "the same shape". The divergences, stated so nobody reads the sibling file as a schema: the sibling has {commit, measured_at, metrics:{name:{mean, stdev, threshold}}, queries_path, queries_sha256, runs, sigma}, a sigma-based band over a continuous score; the temporal file has no mean, no stdev and no sigma, because its metrics are integer counts of named cases, and it adds must_pass_cases, d1_blob_sha256, corpus_sha256 and polish_state. One structural rule IS copied verbatim: check_thresholds.py:133-138 reads the artefact PATH from the DUMP (dump.get("queries_path")) and only the SHA from the thresholds, so the thresholds file never dictates where the artefact lives. The temporal port mirrors that split — the dump carries fixture_ref and the computed digest, the thresholds carry the expected sha. Add the checker with the house exit-code contract: 0 pass, 1 regression, 2 validation error, a check() returning (failures, validation_errors) so a malformed dump fails loudly instead of passing quietly, one FAIL line per failure on stderr. Populate the thresholds from the T1 D1 matrix, which was committed before the first run.
  </objective>
  <files-to-create>
    <file path="docs/specs/temporal-staleness/thresholds.json">{commit, measured_at, metrics: {age_differential_slot1_stable: {measured, threshold}, age_differential_annotation_changed: {measured, threshold}, hits_with_age_in_envelope: {measured, threshold, compare: "eq-hits-total"}}, must_pass_cases: [the sc-prefixed case ids named in D1], d1_blob_sha256 (git hash-object of docs/specs/temporal-staleness/decisions.md at pre-commit, so raising a threshold requires editing D1 and shows in the diff), fixture_ref: "scripts/memory_recall_eval.py::TEMPORAL_SCENARIOS", fixture_sha256 (sha256 of json.dumps of the canonicalised TEMPORAL_SCENARIOS table, sort_keys=True — NOT the whole 416-line script, so unrelated edits by T2/T4/T5 to that same script do not break the gate), fixture_hash_description (a one-line prose statement of exactly what was hashed), corpus_sha256 (sha256 over the CAPTURED on-disk bytes, files sorted by path relative to the tmp root — the source table and the bytes that reach disk are not the same artefact once polish is in the path), polish_state (the pinned cache directory shape and strict setting recorded by T4), runs}. Every threshold is an integer count, never a percentage.</file>
    <file path="scripts/check_temporal_thresholds.py">Stdlib-only gate, modelled on attune-rag/scripts/check_thresholds.py with attune-rag/tests/unit/test_check_thresholds.py as the test template. _load_json, extract_metrics(dump), check(dump, thresholds, verify_fixture_sha256) returning (failures, validation_errors), main() with --dump/--thresholds/--skip-fixture-sha-check. Exit 0 pass, 1 regression, 2 validation error. Exits 2 — never 1, never 0 — when: the dump is missing or unparseable; instrument_ok is false; the dump's polish_state differs from the thresholds'; the dump's generated_at or git_head is absent or does not match the current run; hits_with_age_in_envelope is absent. When --skip-fixture-sha-check is passed it prints a loud SKIPPED line to stderr and sets sha_check_skipped true in its own output, so a skipped binding is never silent. Apache-2.0 footer in the module docstring, since this is a new file.</file>
  </files-to-create>
  <validation>
    <check>python scripts/check_temporal_thresholds.py --dump temporal_dump.json --thresholds docs/specs/temporal-staleness/thresholds.json exits 0, where temporal_dump.json is the --json-out file from a passing --phase temporal run.</check>
    <check>Hand-edit that dump so one must-pass case fails; the script exits 1 and prints exactly one FAIL line naming that case id on stderr.</check>
    <check>Feed a dump missing the hits_with_age_in_envelope key; the script exits 2, not 0 and not 1. Same for a dump with instrument_ok false, and for a dump whose polish_state disagrees with the thresholds.</check>
    <check>Change one character inside the TEMPORAL_SCENARIOS table and re-run against the locked thresholds without regenerating the sha; the script exits 2 with a fixture-mismatch message. Then make an unrelated edit ELSEWHERE in scripts/memory_recall_eval.py — a docstring line outside the table — and re-run: the script still exits 0, proving the binding is to the canonicalised table and not to the file.</check>
    <check>Run with --skip-fixture-sha-check and confirm the SKIPPED line reaches stderr and sha_check_skipped is true in the output. T8 adds the workflow assertion that CI never passes this flag.</check>
  </validation>
  <risks>
    <risk severity="medium">Locking thresholds after the first run inverts the discipline. The numbers come from the D1 matrix committed in T1; if the first run disagrees, the honest move is a dated decisions entry, not an edited threshold. d1_blob_sha256 makes an edited threshold visible as an edited D1.</risk>
    <risk severity="low">Note, not an open question: binding fixture_sha256 to the whole of scripts/memory_recall_eval.py was considered and REJECTED, because T2, T4 and T5 all edit that same script and the gate would break on its own workstream. The ruling above — hash the canonicalised TEMPORAL_SCENARIOS table, plus a separate corpus_sha256 over the captured bytes — is what the files-to-create entry specifies, and there is no second option left open.</risk>
  </risks>
  <dependencies>
    <dep>T1</dep>
    <dep>T5</dep>
  </dependencies>
</task>

<task id="T7" name="Tests for the phase, the scoring, the presentation and the gate">
  <objective>
    Neither eval script has any test today — a grep across tests/ for memory_recall_eval and eval_personal_memory_recall returns nothing, and scripts/ is excluded from ruff, so this work is being built on uncovered, unlinted code. Add coverage for the new surface following the house pattern for scripts: REPO_ROOT = Path(__file__).resolve().parents[3], SCRIPT_PATH = REPO_ROOT / "scripts" / "memory_recall_eval.py", and a module-scoped fixture loading it via importlib spec_from_file_location, module_from_spec, sys.modules registration and exec_module, exactly as tests/unit/scripts/test_ledger_precision.py does; attune-rag/tests/unit/test_check_thresholds.py is the template for the gate-checker tests. For any age assertion use unverified_age_days(mem, today=...) — the today argument is an injectable clock, so these tests need no time freezing and no clock control library. Pin the properties that matter: check_temporal_fixture() returns nonzero AND prints no metric line for each drift shape, fed synthetic scenario objects (expected_winner absent from the capture list; a duplicate (topic, kind); competitors sharing a slug; a slug colliding with CATEGORY_WEIGHTS; a secret-shaped payload) so the real table is never edited; the temporal leaf phases capture-temporal and evaluate-temporal refuse to run without --root; a broken age envelope is a FAILURE even when hit@3 is perfect; a changed slot-1 identity across the utime flip is a FAILURE; and the sc4-injected payload behaviour — assert FIRST that the payload substring appears in hit["excerpt"], THEN that the instruction flag is set, so a truncated excerpt fails as a named fixture problem rather than as a false "flagging is broken"; plus the curated bare imperative that correctly does NOT flag, and the past-the-window payload that correctly does NOT flag. Add a gate test pinning the thresholds file schema, its fixture-sha binding, and the D1 content binding.
  </objective>
  <files-to-create>
    <file path="tests/unit/scripts/test_memory_recall_eval_temporal.py">Loads the script via importlib. Module docstring states which property is pinned and why, in the manner of tests/unit/memory/test_provenance.py. Inherits the autouse hermetic fixture from tests/unit/scripts/conftest.py. Holds the check_temporal_fixture() drift cases over synthetic scenarios, the leaf-phase --root refusal cases, and the two synthetic-results verdict cases from T5.</file>
    <file path="tests/unit/gates/test_temporal_thresholds_schema.py">Pins docs/specs/temporal-staleness/thresholds.json keys; that every threshold is an integer count and not a float in [0,1]; that must_pass_cases is non-empty, is exactly the set of case ids named in the D1 blob whose sha the file records in d1_blob_sha256 (recompute git hash-object over decisions.md and compare, so a threshold raised after the fact requires an editable, diffable D1 change), and matches the case ids in TEMPORAL_SCENARIOS; and that fixture_ref names the table rather than the whole script.</file>
  </files-to-create>
  <files-to-modify>
    <file path="tests/unit/memory/test_provenance.py">Add ONLY the curated-tier by-design non-flag case and the past-the-excerpt-window non-flag case. The wrap_recalled status-argument cases belong to T3 and already exist by the time this task runs — do not re-add them. Keep the existing posture that tests assert framing, never neutralisation.</file>
    <file path="tests/unit/test_cli_memory_commands.py">Pin that cmd_memory_recall's human output carries the staleness label AND that the number of printed result lines is greater than zero, against a tmp root with a known captured hit — so the CLI surface cannot silently drift back to raw prose and cannot pass on "No results found.".</file>
  </files-to-modify>
  <validation>
    <check>python -m pytest tests/unit/scripts/test_memory_recall_eval_temporal.py tests/unit/gates/test_temporal_thresholds_schema.py -q passes.</check>
    <check>python -m pytest tests/unit/memory/ tests/unit/test_cli_memory_commands.py -q passes.</check>
    <check>The broken-envelope test fails if the pass computation in evaluate_temporal is changed to read hit@3 — verify by making that edit locally, watching the test go red, and reverting.</check>
    <check>python -m pytest tests/unit/scripts/test_memory_recall_eval_temporal.py -n 4 passes, confirming parallel safety under the repo's -n auto default.</check>
    <check>grep -n "pytest.mark" on the new test files shows no marker outside pytest.ini's registered list (smoke, slow, integration, unit, security, performance, llm, network, behavioral, cli, refactored) plus pytest's builtins (parametrize, skip, skipif, xfail) — which is exactly what --strict-markers enforces. parametrize in particular is a builtin and appears in no markers list.</check>
  </validation>
  <risks>
    <risk severity="medium">The subprocess-forking temporal orchestrator is slow to run inside a unit test. Test the leaf phases and the scoring functions directly, and cover the orchestrator with one guarded end-to-end case rather than one per scenario.</risk>
    <risk severity="low">pytest.ini sets filterwarnings = error. A new test that triggers a warning outside the four ignored categories fails the suite.</risk>
  </risks>
  <dependencies>
    <dep>T4</dep>
    <dep>T5</dep>
    <dep>T6</dep>
  </dependencies>
</task>

<task id="T8" name="CI wiring, first run, and the dated verdict">
  <objective>
    Wire the gate as a job that runs --phase temporal --json-out and feeds the dump to scripts/check_temporal_thresholds.py. Do NOT port attune-rag/.github/workflows/benchmark.yml's shape wholesale: that workflow wraps the benchmark in a retry loop that on failure prints "marking gate inconclusive (not blocking PR)" and exits 0 (lines 139-143), skips the checker via `if: steps.bench.outputs.inconclusive == 'false'` (line 149), and carries continue-on-error: true on the checker step (line 167) — importing that shape would make a crashing temporal phase read as green CI. memory-temporal.yml has NO inconclusive branch, NO retry-to-green, and NO continue-on-error on the checker step: a temporal-phase crash fails the job. The constraints that actually bite a new workflow file in this repo are per-file and unconditional: TestTimeoutEnforcement::test_every_job_has_timeout (every job needs timeout-minutes), TestSHAPinning::test_all_uses_directives_are_sha_pinned (every uses: is a 40-hex SHA) and test_sha_pinned_actions_have_version_comment (each pinned SHA carries a trailing "# vX.Y.Z" comment), plus tests/unit/ci/test_ci_spend_guard.py's empty-string rule for ANTHROPIC_API_KEY. Concurrency is NOT checked for a new file — WORKFLOWS_REQUIRING_CONCURRENCY at tests/unit/ci/test_workflow_yaml.py:60-72 is a hardcoded seven-name allowlist — so this task adds the filename to that set rather than assuming the requirement applies. There is no permissions test anywhere in test_workflow_yaml.py; `permissions: contents: read` stays as a house convention, not a checked constraint, and this task does not claim otherwise. Then run the benchmark once and write ONE dated decisions.md entry in house style: the per-case matrix outcome against the D1 pre-commit, what the run establishes and explicitly what it does NOT, the conflict-presentation decision as implemented, the sc4-injected result including the by-design non-flags, the gate's landing posture, and the ruling on release_recall_gate.py. Add the CHANGELOG [Unreleased] entry the changelog gate requires, and update the Health checks section of docs/how-to/which-memory-is-which.md, which today names only `attune memory topics` and scripts/release_recall_gate.py.
  </objective>
  <files-to-create>
    <file path=".github/workflows/memory-temporal.yml">Runs the temporal phase and the checker on pull_request and push to main. timeout-minutes on every job, 40-hex SHA-pinned actions each with a trailing "# vX.Y.Z" comment, a concurrency group with cancel-in-progress, permissions contents: read (house convention), ANTHROPIC_API_KEY: "". The checker step carries neither continue-on-error nor any skipping if:, and invokes check_temporal_thresholds.py with neither --skip-fixture-sha-check nor any skip-metric flag.</file>
    <file path="tests/unit/ci/test_memory_temporal_workflow.py">Asserts the properties test_workflow_yaml.py does not check for this file: the checker step has no continue-on-error and no if: that can skip it; the workflow contains no "inconclusive" output or retry-to-green branch; the checker invocation contains neither --skip-fixture-sha-check nor --skip-metric; and "memory-temporal.yml" is a member of tests.unit.ci.test_workflow_yaml.WORKFLOWS_REQUIRING_CONCURRENCY, so the concurrency requirement named in the objective is actually enforced. Also asserts the landing posture is stated: decisions.md contains a line naming the posture as either advisory or required, so the question cannot be left unanswered.</file>
  </files-to-create>
  <files-to-modify>
    <file path="tests/unit/ci/test_workflow_yaml.py">Add "memory-temporal.yml" to WORKFLOWS_REQUIRING_CONCURRENCY (lines 60-72). Without this entry, test_expected_workflows_have_concurrency never sees the new workflow and the suite is green whether or not it has a concurrency group.</file>
    <file path="docs/specs/temporal-staleness/decisions.md">Append one dated entry with the first run's per-case outcome against D1, stating in its own words what the measurement does and does not establish, and refusing to convert the case counts into a rate. It must also state, explicitly and in one sentence each: (1) the gate's LANDING POSTURE — advisory-first is a legitimate answer — the specific condition under which it becomes a required status check, and the named person who flips branch protection; (2) the RULING on whether the temporal round-trip joins scripts/release_recall_gate.py, with the reason either way, so the conditional is closed before any implementation of it starts rather than left to the executor.</file>
    <file path="CHANGELOG.md">An [Unreleased] entry. .github/scripts/changelog_gate.py requires either an entry under [Unreleased] or the "no-changelog" label on the PR, and .github/workflows/changelog-gate.yml runs on every PR with no path filter, deliberately (its lines 8-13 explain why).</file>
    <file path="docs/how-to/which-memory-is-which.md">Extend the Health checks section so it names the temporal gate alongside scripts/release_recall_gate.py.</file>
  </files-to-modify>
  <validation>
    <check>python -m pytest tests/unit/ci/test_workflow_yaml.py tests/unit/ci/test_ci_spend_guard.py tests/unit/ci/test_memory_temporal_workflow.py -q passes with the new workflow present.</check>
    <check>grep -n "memory-temporal.yml" tests/unit/ci/test_workflow_yaml.py returns a line inside WORKFLOWS_REQUIRING_CONCURRENCY. RED before this change, GREEN after.</check>
    <check>grep -nE "continue-on-error|inconclusive|skip-fixture-sha-check|skip-metric" .github/workflows/memory-temporal.yml returns nothing.</check>
    <check>grep -n "ANTHROPIC_API_KEY" .github/workflows/memory-temporal.yml shows the empty-string form, not an unset variable and not a secret reference.</check>
    <check>The workflow's checker step exits 0 on the committed thresholds and the first run's dump, and the run's job log shows the DIAGNOSTIC label on the hit@k and slot1_current_fact_wins lines.</check>
    <check>grep -nE "advisory|required" docs/specs/temporal-staleness/decisions.md returns the posture sentence, and grep -n "release_recall_gate" returns the ruling sentence. Both are read back by tests/unit/ci/test_memory_temporal_workflow.py so the posture cannot be left unstated.</check>
    <check>grep -nE "[0-9]+%" docs/specs/temporal-staleness/decisions.md shows no pass condition stated as a rate.</check>
    <check>grep -n "temporal" docs/how-to/which-memory-is-which.md returns a line inside the Health checks section.</check>
  </validation>
  <risks>
    <risk severity="high">This arms a CI check that can block merges on a handful of hand-authored cases. The memory-recall-eval entry dated 2026-08-28 ruled that a behaviour gate here is PENDING and fixed the precondition order: define the acceptance criterion, expand the negative set, then wire CI. A human must confirm the per-case matrix satisfies that order, and must decide the landing posture and who flips branch protection, before this merges.</risk>
    <risk severity="medium">A gate that exists but is not a required status check is a gate nobody has to satisfy. Advisory-first is legitimate, but only if the decisions entry names the condition that promotes it and the person who makes the change; otherwise the workstream ships a green badge and no enforcement.</risk>
    <risk severity="medium">The first run may disagree with the pre-committed matrix. The rule is to record the disagreement in the dated entry, not to edit D1 — "Numbers revisitable only with stated reason before the run, never after", and d1_blob_sha256 in thresholds.json makes a silent edit fail tests/unit/gates/test_temporal_thresholds_schema.py.</risk>
  </risks>
  <dependencies>
    <dep>T6</dep>
    <dep>T7</dep>
  </dependencies>
</task>

---

## Reference

Everything below is human context. `read_spec()` parses only `<task>` blocks, so nothing here reaches an executor — every load-bearing directive that used to live only in this section has been moved into the task that needs it.

**Files being modified**

- `scripts/memory_recall_eval.py` — 416 lines. `CorpusEntry`, `CORPUS` (18 entries), `Query`, `QUERIES` (18 positive + 5 negative), `capture_corpus(global_root)`, `evaluate(global_root)`, `run_benchmark()`, `run_persistence_benchmark()`, `print_report(results)`, `main()`. `--phase all|persistence|capture|evaluate`, `--root`, `--json-out`. `--json-out` is honoured ONLY inside the `evaluate` branch (lines 399-403); `persistence` and `all` call `print_report()` and ignore it. The `--root` guard is at line 392: `if args.phase in ("capture", "evaluate") and args.root is None`. No licence header (`grep -c Apache` returns 0) — match the file, do not add one mid-change. Module docstring line 20 names `~/.attune/personal_memory`, a path that exists nowhere else in `src/` or `scripts/`; the real default is `~/.attune/memory`.
- `src/attune/memory/personal.py` — `_GLOBAL_ROOT = Path.home() / ".attune" / "memory"` at line 32. `_secret_gate` at ~line 103: a detected critical/high secret RAISES (capture is deliberate; the caller must know and rotate), PII is redacted in place, and the gate fails closed with an error if `DataSanitizer` cannot be imported. `capture()` calls it unconditionally at line 226. `query()` at line 242 sets `"summary"` and `"excerpt"` to the same `hit.excerpt or ""` (~296-297), calls `_annotate_staleness(results)` then `_stamp_provenance(results)`, then `log_curated_recall([...stems...], surface="personal_query")`. `_stamp_provenance` builds provenance from `str(hit.get("excerpt") or hit.get("summary") or "")` at line 342 with `tier="curated"`. `_annotate_staleness` stamps `unverified_days`, `staleness`, `status` and swallows `(KeyError, OSError, ValueError)` at line 378; `_resolve_hit_path` (399-407) returns `None` and the loop `continue`s without setting `status`. `capture()` writes `root/TOPIC/KIND.md` via `atomic_write_text`, which replaces destructively with no versioning.
- `src/attune/memory/provenance.py` — 280 lines. `scan_instructions(text, *, tier=None)` at line 97, `provenance_fields(...)` at line 127, `wrap_recalled(text, *, tier, source, author_class, instruction_flags=None)` at line 167, `render_recall_for_context(...)` at line 213. `UNTRUSTED_TIERS = frozenset({"raw", "machine", "machine-extracted"})` at line 94; `_DIRECTIVE_PATTERNS` fire only on those tiers, so a bare imperative in a curated memory is deliberately not flagged.
- `src/attune/cli_commands/memory_commands.py` — `cmd_memory_recall` prints `hit['score']`, `hit['path']`, `hit['summary']` and `hit['excerpt']` raw at lines ~186-193, with no envelope and no staleness label. On an empty or non-matching corpus it prints "No results found." and returns 0.
- `src/attune/mcp/memory_handlers.py` — `_handle_personal_memory_recall` at line 405 renders `render_recall_for_context(hits)` into `context` and strips `summary` and `excerpt` from every structured result (line 438); fails closed on ImportError. It needs no edit for this work — T3 verifies it by assertion instead.
- `docs/how-to/which-memory-is-which.md` — Health checks section at line 64+, currently naming `attune memory topics` and `scripts/release_recall_gate.py`.
- `tests/unit/ci/test_workflow_yaml.py` — `WORKFLOWS_REQUIRING_CONCURRENCY` (lines 60-72) is a hardcoded set of seven names: tests.yml, pre-commit.yml, changelog-gate.yml, docs.yml, security.yml, security-scan.yml, tier-pattern-analysis.yml. Test classes: `TestSchemaValidation`, `TestTimeoutEnforcement`, `TestConcurrencyControls`, `TestSHAPinning`, `TestPipCaching`, and others. `grep -n permissions` on the file returns nothing.

**Model being reused, not rebuilt**

- `src/attune/memory/curated_audit.py` — 815 lines. `VOLATILITY_BY_TYPE` (project 1.00, reference 0.60, lesson 0.40, feedback 0.15, user 0.10), `DEFAULT_VOLATILITY = 0.75` (line 94), `load_memory(path)` (never raises), `resolve_age_basis(mem, latest_verdict)` (line 361) returning a date and one of verified / verified-unbound / invalidated / tombstoned / mtime, `unverified_age_days(mem, today=None, latest_verdict=None)` (line 401) with an injectable `today` so tests need no clock control, `epistemic_tier()` (line 506) against `TIER_SETTLED_MAX = 10.0` (line 499) and `TIER_CHECK_MAX = 45.0` (line 500), `format_status_annotation()`, `format_age_annotation()`. Already covered by `tests/unit/memory/test_curated_audit.py`.
- `src/attune/memory/verdict_log.py` — `latest_verdicts(root)` mapping stem to `VerdictRecord`, consumed by both `PersonalMemory._latest_verdict_for` and `curated_audit.resolve_age_basis`.

**Why ranking cannot be the gate**

- `attune-rag/src/attune_rag/retrieval.py` — `KeywordRetriever.retrieve` (line ~352) scores on token overlap times `CATEGORY_WEIGHTS` with no mtime, recency or verified term anywhere. `CATEGORY_WEIGHTS` is a class attribute (line 227) aliasing `_DEFAULT_CATEGORY_WEIGHTS` (line 172): concepts 1.5, quickstarts 1.5, tasks 1.2, references 1.0, comparisons 1.0, tips 0.9, and more; `DirectoryCorpus._infer_category` takes the first path segment, which for personal memory is the topic slug. `PersonalMemory.query` sorts on that score plus a +0.001 project tiebreak. A hand-authored slot-1 winner is therefore a function of vocabulary, which is why the gated case is a differential over age with wording held constant.
- `attune-rag/src/attune_rag/provenance.py:193-211` — `excerpt_chars: int = 200`, `excerpt = content.strip()[:excerpt_chars]`; `pipeline.py` (lines 329, 485) never overrides it. With `_build_skeleton`'s `"# {topic}\n\n## Decision\n\n"` prefix, only the first ~180 characters of a captured fixture are ever scanned by `scan_instructions`.
- `src/attune/authoring/polish.py` — `_CACHE_DIR_ENV = "ATTUNE_AUTHOR_POLISH_CACHE"`, `_CACHE_DIR_DEFAULT = Path.home() / ".attune" / "polish_cache"` (lines 66-67), `STRICT_ENV_VAR = "ATTUNE_AUTHOR_STRICT_POLISH"` (line 246). The cache is consulted before any LLM call, so the same table yields polished text on a warm developer machine and skeletons on a cold CI runner unless both are pinned.
- `src/attune/memory/serve_telemetry.py` — writes `$ATTUNE_HOME/telemetry/memory_events.jsonl` (line 64-66), gated by `ATTUNE_MEMORY_TELEMETRY` and `DO_NOT_TRACK`. Every `PersonalMemory.query` call logs to it.

**Prior art being mirrored**

- Multi-process phase architecture: `scripts/memory_recall_eval.py::run_persistence_benchmark()` — one `tempfile.mkdtemp()` root, `subprocess.run([sys.executable, script, "--phase", "capture", "--root", ...], check=True)`, then a second subprocess with `--phase evaluate --json-out`, results read from a file because structlog corrupts stdout, everything torn down in a `finally`.
- Relative, self-calibrating criterion: `scripts/eval_personal_memory_recall.py`, ruled as OQ3 option (a) / D3 in `docs/specs/memory-recall-eval/design.md` — threshold derived within the run from the minimum correct-positive top-1 score, with "criterion inapplicable" reported instead of a fake number in degenerate cases.
- Threshold gate to port: `attune-rag/scripts/check_thresholds.py` (338 lines; `MetricFailure`, `extract_metrics`, `check()` returning (failures, validation_errors), exit 0/1/2, `--skip-metric`, `format_failure_comment()`; lines 133-138 read the artefact path from the DUMP and only the sha from the thresholds), its locked baseline `attune-rag/docs/specs/release-quality-baseline/thresholds.json` (`{commit, measured_at, metrics:{name:{mean, stdev, threshold}}, queries_path, queries_sha256, runs, sigma}` — a sigma band over a continuous score, deliberately NOT the temporal file's shape), its consumer `attune-rag/.github/workflows/benchmark.yml` line 158, and its tests `attune-rag/tests/unit/test_check_thresholds.py`. That workflow's retry-to-inconclusive path (lines 139-149) and `continue-on-error: true` checker step (line 167) are the parts NOT to port. Smallest credible variant: `attune-verify/scripts/mutation_gate.py`.
- Fixture-drift discipline: `scripts/phase0/lessons_rag_benchmark.py` cross-checks every expected slug against the live corpus and returns 1 before reporting any number.
- Release-path gate in this family: `scripts/release_recall_gate.py` (isolated fake `HOME`, env built from scratch with `ANTHROPIC_API_KEY: ""`, wheel install, capture-to-recall round-trip, duplicate-path assertion), wired at `.github/workflows/publish-pypi.yml` line 47. Whether the temporal round-trip joins it is ruled in T8's decisions entry, not left conditional in a files-to-modify line.
- Pre-committed matrix template: `docs/specs/archive/lessons-corpus-rag/decisions.md` D1 — benchmark shape, a three-row Result/Decision table, an explicit note on what is motivational versus a gate, high-severity cases tagged in the fixture before the first run. Sibling clause in `docs/specs/archive/ams-int8-quantization/decisions.md`: "Numbers revisitable only with stated reason before the run, never after." Its "the commit timestamp is the arbiter" formula is deliberately NOT copied — committer dates are rewritten by rebase and amend and settable via `GIT_COMMITTER_DATE`, so D1 binds to its blob sha instead.
- Test-a-script pattern: `tests/unit/scripts/test_ledger_precision.py` (importlib loading via `spec_from_file_location`) and `tests/unit/scripts/conftest.py` (autouse hermetic ledger fixture).
- Test posture to copy: `tests/unit/memory/test_provenance.py` — "assert framing and labelling, never that a payload is neutralised", plus `test_directive_flagged_only_for_untrusted_tier` and `test_curated_dev_imperatives_are_not_flagged`, which pin the curated non-flag as intentional.
- mtime forgery precedent: `os.utime` in roughly 20 test modules, e.g. `tests/unit/ops/test_memory_attention.py` line 35 and `tests/unit/ops/test_completion_candidates.py` line 137.
- Spec-directory reminder: `.github/workflows/spec-status-reminder.yml` — on a MERGED PR only, `SPEC_REF = re.compile(r"\b((?:docs/)?specs/[a-z0-9][a-z0-9-]*)\b")` (line 58) scans the PR body, commits and changed paths and posts ONE reminder comment per in-flight spec. A reminder, not a gate, and the only thing in CI that keys off a spec directory name — `.github/scripts/changelog_gate.py` has no spec-directory logic at all.
- Human-review half of the protocol, already shipped: `scripts/review_curated_memory.py` (keep / wrong / sharper, queue capped at 3) and `scripts/audit_curated_memory.py` ("Advisory only. This script never writes to a corpus and always exits 0 — it is not a gate.").
- The only shipped abstention mechanism: `plugin/hooks/lesson_recall.py` filters on `h.score >= floor` with floor 8.0 from `ATTUNE_LESSON_RECALL_FLOOR`. `PersonalMemory` has no floor at all. If conflict presentation ever needs a suppression lever, this is the precedent and its measured failure shape.

**Governing text**

- `attune-ai-dev/discipline/COLLABORATION_DISCIPLINE.md` §5, lines ~684-710 — the two-layer protocol, ratified 2026-07-02 as D6, with the receipt at line 157 and lines 1133-1134. Draft source: `docs/process/COLLABORATION_DISCIPLINE_revision_2026-07-02_proposal.md` §C3, receipt [R-B].
- `docs/specs/memory-status-integrity/requirements.md:167` — R2, "Every recall surface displays unverified-age" ("a reader must see how long it has been since a claim was confirmed"). requirements.md line 338 cross-references D1 but does not contain it.
- `docs/specs/memory-status-integrity/decisions.md:8` — D1, "Label, never suppress by age (recorded 2026-08-07)". This is the file that rules the behaviour T3's objective and T5's differential both lean on.
- `docs/specs/memory-recall-eval/decisions.md` — 584 lines, 11 dated entries (`grep -c '^## 20'` returns 11, at lines 11, 67, 104, 137, 161, 194, 254, 281, 315, 324 and 525). The 2026-08-28 chair-directed re-run sets the precondition order for arming a behaviour gate, forbids reading small counts as rates, and records that "before #2118 polish never ran, and run 1's corpus was UNPOLISHED".
- `src/attune/ops/spec_lifecycle.py:52` — `STATUS_VOCABULARY = _APPROVED_STATUSES | {draft, parked, paused}`, where `_APPROVED_STATUSES = _COMPLETE_STATUSES | {approved, active, living}` and `_COMPLETE_STATUSES = {complete, completed, done, shipped, superseded}`. Enforced over every spec directory by `src/attune/gates/lifecycle/status_line.py` via `tests/unit/gates/test_status_line_gate.py::test_corpus_sweep_every_spec_dir_is_legible`, which parametrizes over every directory under `docs/specs/` carrying one of `_PHASE_FILES` (`src/attune/ops/specs_data.py:26`).
- `AGENTS.md` principle 6 (CI spends attention, never money), 15 (degrade gracefully around the memory layer), 16 (claims carry their basis). Session protocol: run `python scripts/collaboration_preflight.py` before non-trivial work.

**Constraints that will bite**

- `pytest.ini`: `--strict-markers`, `-n auto`, `filterwarnings = error`, `pythonpath=src`. Registered markers: smoke, slow, integration, unit, security, performance, llm, network, behavioral, cli, refactored. `parametrize`, `skip`, `skipif` and `xfail` are pytest builtins and appear in no markers list — `--strict-markers` accepts them. Tests must be parallel-safe and must not write to `$HOME`.
- `pyproject.toml`: `[tool.ruff]` exclude includes `scripts/`, so CI catches no style problem in the eval script. `B904` is enforced in `src/` — `raise ... from e`.
- Licence footer (`Copyright <year> Smart AI Memory, LLC` / Apache-2.0) goes in the module docstring of NEW files only; `scripts/memory_recall_eval.py` has none and must not gain one mid-change, while `scripts/check_temporal_thresholds.py` is new and takes one.
- `.github/workflows/changelog-gate.yml` runs on every PR with no path filter, deliberately (lines 8-13 record why a path-filtered required check blocks PRs forever).